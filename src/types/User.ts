// src/types/User.ts
export type User = {
    uid: string;
    name: string;
    email?: string;
    phoneNumber?: string;
    photoURL?: string;
    description?: string;
    // Add other fields as needed
  };
  